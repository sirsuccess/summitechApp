import React, { Component } from "react";


// import todo from "./todo"
class Post extends Component {
  state = {
    

   
  };

  render() {

    
    



    return (
  
      
      <div>
      
            {/* <h1>Val stories</h1>
            {this.state.posts.map(post => <li className="card" key={post.id}>{post.title} post by: {post.userName} <button className="btn btn-primary" onClick={this.Modal}> view</button> <button onClick={onLike} className="btn btn-primary" > like</button>{post.like}
            </li>)}

            <button>tell your story</button><br/> */}
            
        
        </div>
      
    );
  }

}

export default Post;